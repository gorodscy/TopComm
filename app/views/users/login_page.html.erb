<div class="container form-signin">
<h1>Login</h1>

<%= form_for(:user, url: login_path) do |f| %>
    <%= f.label :username, class:'sr-only' %>
    <%= f.text_field :username , class:'form-control', placeholder:'Usuário' %>
    <%= f.label :password, class:'sr-only' %>
    <%= f.password_field :password, class:'form-control', placeholder:'Senha' %>
  <div class="actions">
    <%= f.submit "Log in" %>
  </div>
<% end %>

</div>
<%= link_to '<span class="glyphicon glyphicon-chevron-left" aria-hidden="true"></span> Voltar'.html_safe, root_path, class:"btn btn-default" %>
